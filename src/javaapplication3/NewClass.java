/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package javaapplication3;

/**
 *
 * @author thiago.dias
 */
public class NewClass {
            // boolean inicio = true;
            // int posicao = -1; 
            // String pesquisa = "";
            // ForcaBruta.ultimaPosicao = 0;
            // for (int ultimaPosicao = -1; ultimaPosicao < mainPublic.texto.getText().length() - txtPesq.getText().length() +1;) { // -1
                
                // inicio = false;
                /*
                if (!inicio) {
                    if (pesquisa == "") {
                        pesquisa =  mainPublic.texto.getText();
                    } else {
                        pesquisa =  pesquisa.substring(posicao + txtPesq.getText().length());
                    }
                    posicao = ForcaBruta.forcaBruta(txtPesq.getText(), pesquisa);
                    // ultimaPosicao = ForcaBruta.ultimaPosicao + ultimaPosicao - 1;
                    ultimaPosicao = ForcaBruta.ultimaPosicao + 1;
                    if (posicao > -1) {
                        mainPublic.colorirTexto(posicao + 1, txtPesq.getText().length());
                    }
                } else {
                    // Primeira execução
                    pesquisa =  mainPublic.texto.getText();
                    posicao = ForcaBruta.forcaBruta(txtPesq.getText(), pesquisa);
                    ultimaPosicao = ForcaBruta.ultimaPosicao;
                    inicio = false;
                    if (posicao > -1) {
                        mainPublic.colorirTexto(posicao, txtPesq.getText().length() - 1);
                    }
                }
                System.out.print("Ultimo de posição: ");
                System.out.println(ultimaPosicao);
                System.out.print("Pesquisar até: ");
                System.out.println(mainPublic.texto.getText().length() - txtPesq.getText().length() +1); // -1
                */
            // }
    
    // Segunda tentantiva
     /* do {
                pesquisa = pesquisa.substring(posicao + 1);
                posicao = ForcaBruta.forcaBruta(txtPesq.getText(), pesquisa);
                if (pesquisa.length() != mainPublic.texto.getText().length()) {
                    marcados.add(posicao + marcados.get(marcados.size() - 1) + 1);
                } else {
                    marcados.add(posicao);
                }
                
            } */ 
    
    
            // if (posicao > -1) { trocar por foreach (marcado)
                // mainPublic.colorirTexto(posicao, txtPesq.getText().length());
            // }
}
